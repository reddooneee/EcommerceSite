document.addEventListener("DOMContentLoaded", () => {
    const button = document.getElementById("btn-cart");
    button.addEventListener("click", () => {
      alert("Spiacenti ma il prodotto selezionato ancora non è disponibile");
    });
  });